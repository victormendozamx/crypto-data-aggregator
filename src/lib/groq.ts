/**
 * Groq API utilities stub
 * AI features are disabled in this version
 */

export function isGroqConfigured(): boolean {
  return false;
}

export async function promptGroqJson<T>(_prompt: string, _schema?: unknown): Promise<T | null> {
  return null;
}
