/**
 * Sound utilities for notifications
 */

export function playNotification(_type: string = 'default'): void {
  // Sound notifications are disabled
  // In a full implementation, this would play a notification sound
}
